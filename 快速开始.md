# 衍图 · 快速开始

衍图（YANTU STUDIO）是一个本地运行的 AI 影视 / 短剧创作工作台：自由画布 + 结构化分镜 + 图片/视频/音频生成 + Codex 画布 Agent。

## 一、环境要求

| 项目 | 要求 |
| --- | --- |
| 系统 | Windows 10 / 11 |
| Node.js | 18 或更高（含 npm） |
| 浏览器 | Chrome / Edge |
| Go | **仅源码方式需要**（1.21+）；使用「免构建包」不需要 |

> 首次运行会自动通过 npm 安装 `bun`（前端与 Agent 的构建/运行工具）。

## 二、启动

1. 解压到任意目录（路径避免中文与空格）
2. 双击 `start-yingce.cmd`
3. 浏览器打开 <http://localhost:3000>
4. **第一次使用请点「注册」创建管理员账号** —— 数据库为空时第一个注册的用户自动成为管理员

停止服务：双击 `stop-yingce.cmd`

## 三、配置模型

进入「设置 → 模型渠道」，填入你自己的 API Key（文本 / 图片 / 视频 / 音频渠道各自配置）。
衍图不自带任何密钥，所有生成都走你自己配置的渠道。

## 四、目录说明

```
衍图
├─ web/            前端（Vite + React）
├─ backend/        后端（Go，自带 server.exe 时无需编译）
├─ canvas-agent/   画布 Agent（MCP，内置 dist 时无需编译）
├─ plugins/yingce/ Codex 插件：让 Codex 读写画布、触发生成
├─ scripts/        启动 / 停止脚本
└─ .local/         本地运行数据（首次启动自动生成，含账号、Agent token、渠道 Key、画布与素材）
```

## 五、端口

| 服务 | 地址 |
| --- | --- |
| 前端 | http://localhost:3000 |
| 后端 | http://127.0.0.1:8080 |
| Canvas Agent | http://127.0.0.1:17371 |

## 六、注意

- `.local/` 里是你的账号凭据、Agent token、模型渠道 Key 与画布数据，**不要连同 `.local/` 一起打包分享**。
- 端口被占用时，先运行 `stop-yingce.cmd` 再重新启动。
- 日志在 `.local/runtime/` 下（`backend.out.log`、`web.out.log`、`canvas-agent.out.log`）。
